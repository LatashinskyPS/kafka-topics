consumer:
  bot:
    token:
      ${bot-token:5985835126:AAFuu_qlP0StK7Jx49Fkyp97Nh1aaHVPiSE}
spring:
  kafka:
    bootstrap-servers:
      ${kafka-bootstrap-server:localhost:9092}
    concurrency:
      ${consumer-concurrency:3}
max-retry:
  ${max-event-retry:5}
topics:
  main:
    ${basic-topic:kafka-test-topic}
  retry:
    ${retry-topic:${topics.main}-retry}
  dlq:
    ${dlq-topic:${topics.main}-dlq}
  event-back:
    ${event-back-topic:${topics.main}-back}
